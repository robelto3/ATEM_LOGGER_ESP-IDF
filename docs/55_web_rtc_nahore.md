# Home: RTC datum a čas nahoře

Na stránce Home je datum a čas z RTC zobrazený jako první řádek pod nadpisem `Home`.

Prefix `RTC:` byl odstraněn, takže se zobrazuje pouze samotná hodnota, například:

```text
12.05.2026 18:42:10
```

Živé obnovování přes `/api/state` zůstává zachované.
